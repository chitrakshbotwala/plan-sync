All MVVM pending items resolved.

Remaining in `lib/widgets/` (shared across features — leave for later):
- `bottom-sheets/` — used from multiple features + app_tour_service
- `dropdowns/` — filter-driven, used from bottom-sheets
- `popups/` — used from schedule, settings, version features
- `scaffold_with_nav_bar.dart` — app-level navigation shell
- `tutorials/` — used from app_tour_service
